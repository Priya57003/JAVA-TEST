#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    char str[50];
    scanf("%s", str);  // Use %s to read a string

    for (int i = 0; i < strlen(str); i++) {
        if (str[i] >= '0' && str[i] <= '9') {
            printf("%c", str[i]);  // Print characters, not integers
        }
    }

    return 0;
}

