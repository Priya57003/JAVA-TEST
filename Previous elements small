#include <stdio.h>
#include <limits.h>
int main()
{
    int min=INT_MAX;
    int n=5;
    int check;
    for(int i=0;i<n;i++)
    {
        scanf("%d",&check);
        if(check<min)
        {
            min = check;
            printf("%d ",min);
        }
    }

    return 0;
}
